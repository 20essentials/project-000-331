<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Background Mtr</title>
  <meta name="google" content="notranslate" />
  <link rel="shortcut icon" href="assets/favicon.webp" type="image/x-icon" />
  <style>
    *,
    *::after,
    *::before {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: Verdana, Geneva, Tahoma, sans-serif, system-ui, -apple-system,
        BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell,
        'Open Sans', 'Helvetica Neue';
    }

    :root {
      --primary-color: darkorange;
      --secondary-color: #0a8;
    }

    body {
      height: 100vh;
      width: 100%;
      display: flex;
      flex-wrap: wrap;
      place-content: center;
      overflow: hidden;
      background-image:
        {% assign positions = "20 20,50 20,70 20,20 50,50 50,70 50,20 70,50 70,70 70" | split: "," %}
        {% for pos in positions %}
          repeating-conic-gradient(
            from var(--deg) at {{ pos | replace: ' ', '%' }}%,
            {% if forloop.first %}
              var(--primary-color) 0 1deg,
            {% else %}
              var(--secondary-color) 0 1deg,
            {% endif %}
            transparent 2deg 3deg
          ){% unless forloop.last %},{% endunless %}
        {% endfor %}
      ;
      animation: rotar 120s linear infinite alternate both;
    }

    @keyframes rotar {
      0% {
        --deg: 0deg;
      }
      100% {
        --deg: 360deg;
      }
    }

    @property --deg {
      syntax: '<angle>';
      inherits: true;
      initial-value: 0deg;
    }
  </style>
</head>
<body></body>
</html>
